<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_loaned %>
    <%= form.date_select :date_loaned %>
  </div>

  <div class="field">
    <%= form.label :date_returned %>
    <%= form.date_select :date_returned %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
